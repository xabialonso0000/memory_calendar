# プロジェクト状況整理

最終確認日: 2026-08-01

最新更新: 2026-08-01（高優先度項目を実装）

## 1. 概要

日記とスケジュールを管理するWebアプリです。

- フロントエンド: React 18 / TypeScript / Vite / Material UI / React Quill
- バックエンド: FastAPI / SQLAlchemy / SQLite
- 実行環境: Docker Compose
- API接続先: `http://localhost:8000/api`

## 2. 現在の実装状況

### 日記機能

- [x] 日記一覧の取得・表示
- [x] 日記の新規登録
- [x] 日記の編集
- [x] 日記の削除
- [x] 削除前の確認ダイアログ
- [x] リッチテキスト編集
- [x] エディタからの画像アップロード・挿入
- [x] 日付の表示
- [ ] 日記の検索（現行画面では確認できない）
- [ ] ページネーション（現行画面では確認できない）

### スケジュール機能

- [x] バックエンドの一覧取得API
- [x] バックエンドの新規登録API
- [x] バックエンドの詳細取得API
- [x] バックエンドの編集API
- [x] バックエンドの削除API
- [x] データベースモデルとスキーマ
- [x] フロントエンドでの一覧表示
- [x] カレンダー表示
- [x] カレンダーからの新規登録
- [x] フロントエンドからの編集・削除
- [ ] 日記・スケジュールの横断検索

### UI/UX

- [x] Material UIを利用した基本レイアウト
- [x] 日記の作成・一覧・編集画面
- [x] 画面幅に応じて切り替わるGridレイアウト
- [x] カレンダー中心のレイアウト
- [ ] 選択日の詳細表示
- [ ] UI文言の日本語化（現在は主に英語）
- [ ] エラーや成功結果の画面上での分かりやすい通知

### 開発・実行環境

- [x] フロントエンド用Dockerfile
- [x] バックエンド用Dockerfile
- [x] Docker Compose設定
- [x] Jest / Testing Libraryの設定
- [x] 現行画面に合ったフロントエンドテスト
- [ ] バックエンドAPIの自動テスト
- [ ] READMEへのセットアップ・起動・テスト手順の記載

## 3. TODO.mdとの相違

`TODO.md`は過去の達成状況を含んでおり、現在のコードと完全には一致していません。

| TODO.mdの項目 | 現在のコード |
|---|---|
| 日記の編集 | 実装あり |
| スケジュールの編集 | APIはあるが、現行UIにはない |
| 日記・スケジュールの検索 | 現行UIにはない |
| ページネーション | 現行UIにはない |
| カレンダーから詳細設定 | 現行UIにはない |
| カレンダーから新規登録 | 未実装 |
| ブログ形式での日記表示・編集 | 実装あり |
| 画像挿入 | 実装あり |
| カレンダー中心のレイアウト | 現行UIにはない |
| テストコード | ファイルはあるが、現行UIと不整合 |

特に、`frontend/src/ScheduleCalendar.tsx`はGit上で削除状態になっており、`App.tsx`にも `Calendar was here` というコメントだけが残っています。カレンダー関連機能は、一度実装された後に現在の作業状態で外された可能性があります。

## 4. API一覧

### 日記

| メソッド | パス | 用途 |
|---|---|---|
| GET | `/api/entries` | 一覧取得 |
| POST | `/api/entries` | 新規登録 |
| GET | `/api/entries/{id}` | 1件取得 |
| PUT | `/api/entries/{id}` | 編集 |
| DELETE | `/api/entries/{id}` | 削除 |
| POST | `/api/upload` | 画像アップロード |

### スケジュール

| メソッド | パス | 用途 |
|---|---|---|
| GET | `/api/schedule` | 一覧取得 |
| POST | `/api/schedule` | 新規登録 |
| GET | `/api/schedule/{id}` | 1件取得 |
| PUT | `/api/schedule/{id}` | 編集 |
| DELETE | `/api/schedule/{id}` | 削除 |

## 5. 判明している問題・要確認事項

### 優先度: 高（2026-08-01対応済み）

1. **カレンダー機能が現行UIからなくなっている** — 対応済み
   - 月・週・日・予定表示に対応したカレンダーを追加しました。
   - 日時範囲の選択による登録と、予定選択による編集・削除ができます。

2. **テストが現行UIと一致していない** — 対応済み
   - `App.test.tsx`を現在の日本語画面とカレンダー構成に合わせて更新しました。

3. **日記本文のHTMLを直接描画している** — 対応済み
   - DOMPurifyでサニタイズしたHTMLだけを表示するよう変更しました。

4. **アップロードファイル名をそのまま保存している** — 対応済み
   - UUIDによる一意な名前、画像形式とファイル署名の検証、5 MBの上限を追加しました。

### 優先度: 中

5. **依存関係のバージョンが固定されていない**
   - バックエンドの`requirements.txt`にバージョン指定がありません。
   - Pydanticのバージョンによっては、現在の`orm_mode`設定との互換性確認が必要です。

6. **API URLがソースコードに固定されている**
   - 開発・本番環境の切り替えができるよう、Viteの環境変数に移すのが適切です。

7. **READMEの情報が不足している**
   - 現在はプロジェクト名のみで、起動方法や必要条件が記載されていません。

8. **エラー処理が最低限**
   - 多くの失敗はコンソール出力だけで、ユーザー向けの詳細な通知がありません。

### 優先度: 低

9. **生成物がGitの変更対象になっている**
   - `frontend/dist`内に削除・追加されたビルド成果物があります。管理方針の確認が必要です。

10. **未追跡ファイルがある**
    - `backend/uploads/`と`test.py`が未追跡です。アップロード画像や一時ファイルをGit管理するか決める必要があります。

## 6. 現在のGit作業状態

未コミットの変更が複数あり、改修途中の状態です。

- バックエンド、フロントエンド、Docker設定、`TODO.md`に変更あり
- `frontend/src/ScheduleCalendar.tsx`は削除状態
- `frontend/package-lock.json`は削除状態
- `frontend/dist`のビルド成果物が入れ替わっている
- `backend/uploads/`と`test.py`が未追跡

既存の変更は利用者の作業内容として保持し、目的を確認せずに破棄しないことが重要です。

## 7. 推奨する次の作業順

1. 日記とスケジュールの検索・ページネーションを再実装する
2. バックエンドAPIテストを追加する
3. 依存バージョンとDocker構成を検証・固定する
4. READMEに起動方法、テスト方法、構成を記載する
5. TODO.mdを実装状況に合わせて更新する

## 8. 補足

フロントエンドの本番ビルド、Jestテスト、バックエンドのPython構文検証は成功しています。ブラウザと実データベースを使った結合動作はまだ未確認です。
